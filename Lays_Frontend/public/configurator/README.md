Drop the Configurator build files here so they are served at `/configurator`.

Expected structure:

- `index.html` (entry file)
- `public/` or `assets/` (any static files)
- `src/three/scene.js` (or compiled JS files)

After copying the files, open the Vue app and navigate to `/configurator`.
